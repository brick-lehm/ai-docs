# 権限を与えるほど誤りの影響が大きくなる

## Background / Context（背景・文脈）

AIを中心とした開発では、読み取りだけでなく、ブランチ作成、マージ、デプロイ、データ変更までをAIへ任せる選択肢があります。


## Problem（問題）

LLMのもっともらしい誤り、曖昧な指示の解釈、外部テキストの影響は、読み取りだけなら限定的です。
しかし AI にコミット、マージ、デプロイ、データ変更の権限を与えると、同じ誤りが実際の変更として実行されます。


AIを中心とした開発では自動化を進めるほど、この「判断の誤り」と「操作の権限」が結び付く点に注意が必要です。


### AIを中心とした開発で起きること

```text
誤った理解 または 誘導
       ↓
広すぎる権限
       ↓
本番変更・情報漏えい・削除
```

便利さのために一つのAIへ包括的な権限を渡すと、失敗時の被害範囲も包括的になります。


## Goal（達成したいこと）

AIの作業速度を活かしながら、誤った判断や誘導が現実の被害へ直結しない権限設計にすることです。


## No-goal（達成に含めないもの）

すべての操作で人間の承認を要求することや、低リスクの自動化を禁止することは含めません。


## Requirements（要件）

- 操作ごとに最小権限を割り当てられること。

- 高リスク操作を承認または拒否できること。

- 変更履歴と監査記録を残せること。


## Proposed Design（提案する設計）

- 読み取り、ブランチ作成、マージ、本番デプロイを別権限にする。

- 高リスク操作は人間の承認、複数人確認、または実行不可にする。

- 一時的で対象が限定された認証情報を使う。

- 実行後に変更履歴と監査記録を必ず残す。

