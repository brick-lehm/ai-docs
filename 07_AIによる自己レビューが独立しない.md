# AIによる自己レビューが独立しない

## Background / Context（背景・文脈）

実装AIとレビューAIを分けても、同じ要件、設計、会話履歴を与えると、同じ前提から判断する状況です。


## Problem（問題）

AIが生成とレビューの両方を担っても、同じ文脈・同じ設計・似た推論の癖を共有していれば、同じ見落としを繰り返します。
役割名を「実装AI」と「レビューAI」に分けただけでは、独立した検証になりません。


これは人間が自分で書いた文章の誤字を見落としやすいのと似ています。
生成時に自然だと判断した前提を、レビュー時にも自然だと判断しやすいためです。


### AIを中心とした開発で起きること

実装AIが `user_id` を使い、レビューAIが「設計どおり」と判定しても、本来必要なのが `account_id` なら問題は残ります。
成果物との整合性だけを確認すると、前提の誤りは検出できません。


## Goal（達成したいこと）

実装との一致だけでなく、前提・設計・既存制約の誤りも発見できるレビューにすることです。


## No-goal（達成に含めないもの）

すべての変更へ複数モデルや複数人のレビューを必須にすることは含めません。


## Requirements（要件）

- 実装準拠と設計妥当性を別の観点で確認できること。

- 要件、反例、既存制約をレビュー入力に含められること。

- 高リスク変更では独立した確認者を置けること。


## Proposed Design（提案する設計）

- レビューは「設計に従ったか」と「設計自体が妥当か」を分ける。

- レビュアーには実装の説明だけでなく、要件・既存制約・反例を渡す。

- 「この案が間違っているなら、どこが危ないか」を必ず検討させる。

- 重要変更は、異なる観点を持つ人間または独立した担当者が確認する。

