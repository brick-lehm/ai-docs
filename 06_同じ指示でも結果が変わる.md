# 同じ指示でも結果が変わる

## Background / Context（背景・文脈）

AIを中心とした開発では、同じ作業を異なる日・異なるモデル・異なるセッションで実行し、成果物を継続的に更新します。


## Problem（問題）

LLMは次の語を一つの正解として固定しているのではなく、候補の確率分布から出力を選びます。
このため、同じ依頼でも API 形式、データ構造、実装順序などが変わることがあります。
設定やモデルの更新でも結果は変わります。


### AIを中心とした開発で起きること

昨日のAIが `PATCH` を提案し、今日のAIが `POST` を提案することがあります。
途中でやり直すと設計が揺れ、レビューの基準も不安定になります。
障害時に「なぜこの変更になったか」を再現しにくいことも問題です。


## Goal（達成したいこと）

生成結果が変動しても、重要な設計判断と変更理由を再現・説明できる状態にすることです。


## No-goal（達成に含めないもの）

LLMの出力を完全に固定することや、探索的な提案を禁止することは目的にしません。


## Requirements（要件）

- 入力、設定、設計判断、検証結果を追跡できること。

- 既存の規約とテストを変更の制約にできること。

- 重要な判断を人間が確定できること。


## Proposed Design（提案する設計）

- 要件、設計判断、実行コマンド、検証結果を成果物として残す。

- 重要な設計は、生成結果ではなく人間が確定した決定として記録する。

- 繰り返し実行する処理は、入力・モデル・設定・出力を追跡可能にする。

- 自由な生成に任せず、既存の規約・型・テストを制約として使う。

