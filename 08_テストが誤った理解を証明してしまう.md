# テストが誤った理解を証明してしまう

## Background / Context（背景・文脈）

AIが要件の解釈、実装、テスト作成を同じセッションまたは同じ設計成果物から行う状況です。


## Problem（問題）

LLMは実装とテストを同じ理解から作れます。
そのため、テストが通っても「AI自身の解釈どおりに動く」ことしか示さない場合があります。
テストの成功は、要求の正しさを自動的には保証しません。


たとえば AI が「常に20%引き」と実装し、その期待値でテストを書いたとします。
実際の規則が「20%引き、ただし上限100円」なら、テストは通っても仕様違反です。


### AIを中心とした開発で起きること

```text
同じ誤解
 ├─ 実装
 └─ テスト
      ↓
    成功
```

テストが多いほど安心に見えるため、誤った前提が見えにくくなる危険があります。


## Goal（達成したいこと）

テストの成功を、AIの解釈ではなく利用者の受け入れ条件を満たす証拠に近づけることです。


## No-goal（達成に含めないもの）

自動テストを減らすことや、すべてのケースを手作業で確認することは目標にしません。


## Requirements（要件）

- 受け入れ条件を実装前に確定できること。

- 実装と独立した観点でテストケースを選べること。

- 例外、境界値、統合時の振る舞いを確認できること。


## Proposed Design（提案する設計）

- 受け入れ条件を実装前に、業務担当者と確定する。

- テストケースは実装から逆算せず、利用者の行動と期待結果から作る。

- 境界値、例外、過去障害、契約互換性を独立した観点として含める。

- 本番に近いデータ・統合テスト・利用者確認で、単体テストを補う。

