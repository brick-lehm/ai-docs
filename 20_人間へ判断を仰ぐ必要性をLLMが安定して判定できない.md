# 人間へ判断を仰ぐ必要性をLLMが安定して判定できない

## Background / Context（背景・文脈）

AIを中心とした開発では、LLMに「このドキュメントに書かれていないことを憶測で進めないでください」と指示し、不明点があれば人間へ確認することを期待します。


しかし、実際にはLLMが不足情報をもっともらしい前提で補い、質問せずに要件、設計、実装、テストを進めることがあります。


## Problem（問題）

LLMは、人間へ判断を仰ぐ必要があるかを、安定して判定する仕組みを標準では持ちません。

LLMは、文脈から次に有用そうな出力を生成します。

そのため、不足情報を「停止すべき未決定事項」ではなく、「一般的な既定値や自然そうな前提で埋める箇所」として扱いやすいです。


「憶測で進めない」という指示だけでは、次の区別を保証できません。


```text
本当に文書に書かれていない事実
別資料に書かれている事実
利用者が当然の前提として省略した事実
既存コードや規約から確認できる事実
業務上の判断者だけが決められる事実
```

また、AIにとっては、確認質問をして止まることと、作業を進めて有用な案を出すことが競合します。

作業の完了を強く求める環境では、AIは質問よりも推測した解決案を出しやすくなります。


### AIを中心とした開発で起きること

```text
要求・設計資料に判断材料がない
        ↓
AIが一般的な前提を置く
        ↓
要件・設計・実装・テストが一貫して完成する
        ↓
人間が確認すべき業務判断が成果物の中に埋もれる
        ↓
運用後に前提の誤りが判明する
```

人間へ確認するためのAIレビューを追加しても、同じ文脈と同じ前提を共有すれば、確認不要だと同じように判断する可能性があります。


## Goal（達成したいこと）

LLMが自発的に質問することへ依存せず、未決定事項が残る変更を人間が判断する前に進めないようにすることです。


## No-goal（達成に含めないもの）

すべての不明点でAIを停止させることや、低リスクな既存規約の適用まで人間へ確認させることは含めません。

また、LLMが確認質問をする能力を否定することも目的にしません。


確認質問は有用ですが、安全性を保証する機構としては十分ではありません。


## Requirements（要件）

- 明記された事実、前提、未決定事項を別々に記録できること。

- 未決定事項ごとに、影響範囲、判断者、期限、実装を止める必要性を記録できること。

- 高リスクな未決定事項が残る場合、変更・マージ・デプロイを機械的に止められること。

- AIが参照した根拠と、参照していない関連領域を提示できること。

- 既存規約で自動判断してよい範囲を人間が事前に定義できること。


## Proposed Design（提案する設計）

- AIには、実装前に「明記された事実」「置いた前提」「未決定事項」を分けて出力させます。

- 未決定事項には、判断者、影響範囲、選択肢、実装を止める必要性を必須項目として持たせます。

- 高リスクな未決定事項が一つでも残る場合、AIに実装権限を与えず、人間の決定を待ちます。

- 完了条件を「コードとテストがある」ではなく、「根拠、前提、未決定事項、判断結果が記録されている」に変更します。

- 人間が事前に決めた既存規約の範囲だけは、AIが確認なしで適用できるようにします。

- AIレビューは未決定事項の候補発見に使いますが、最終的な停止判定は構造化された状態と工程の規則で行います。


## 参考

- [人間とLLMの確認質問行動を比較した研究](https://aclanthology.org/2025.crac-1.1/) — LLMは対象の曖昧さには質問しやすい一方、作業上の不確実性では確認質問が少ないことを報告しています。

- [要求の明確化を用いたLLMコード生成の研究](https://doi.org/10.1145/3660810) — 曖昧な要求に対して確認質問を生成する専用の枠組みを提案しています。

- [LLMが何をいつ尋ねるかを評価する研究](https://www.microsoft.com/en-us/research/publication/do-llms-know-what-to-ask-and-when-evaluating-multi-turn-information-seeking/) — 不足情報の検知、質問内容、得た情報の利用を別々に評価する必要性を扱っています。

