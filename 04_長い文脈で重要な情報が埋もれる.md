# 長い文脈で重要な情報が埋もれる

## Background / Context（背景・文脈）

大規模なリポジトリ、長い会話、複数の設計資料をAIへ渡すと、必要な情報は増える一方で、読む量と依存関係も増えます。


## Problem（問題）

LLMには一度に扱える文脈量の上限があります。
また、文脈に情報が入っていても、すべてを同じ重要度で扱えるわけではありません。
Transformer の Attention は関連情報に注目する仕組みですが、巨大な資料の中にある一行の例外が十分に重視されないことがあります。


### AIを中心とした開発で起きること

大規模なリポジトリでは、AIは必要そうなファイルを選んで読みます。
注文処理を直す際に決済とスキーマだけを読み、会計サービスが同じテーブルを直接参照していることを見落とす、といったことが起きます。


その結果、局所テストは通っても、システム全体では壊れます。


## Goal（達成したいこと）

変更に必要な制約と依存関係を、AIと人間が取りこぼさず確認できるようにすることです。


## No-goal（達成に含めないもの）

全リポジトリや全履歴を毎回AIへ渡すこと、情報量だけで正しさを保証することは目標にしません。


## Requirements（要件）

- 重要な不変条件と例外を短く参照できること。

- 変更の依存先と影響先を確認できること。

- 自動検証で見落としの一部を補えること。


## Proposed Design（提案する設計）

- 重要な不変条件と例外を、短く参照しやすい文書へ集約する。

- 変更前に、参照元・参照先・契約・監視項目の影響範囲を列挙する。

- 大きな変更は、要約だけでなく関係する一次資料に戻って確認する。

- 自動テスト、静的解析、契約テストで、文脈の見落としを補う。

