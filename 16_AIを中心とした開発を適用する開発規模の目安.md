# 現状、AIを中心とした開発を適用する開発規模の目安

## Background / Context（背景・文脈）

AIを中心とした開発では、AIが計画・実装・テストを高速に進め、人間が目的・設計・リスク・本番判断を担います。
開発規模が大きくなるほど、扱う文脈、依存関係、判断者が増えます。


## Problem（問題）

AIへどこまでの大きさの変更を一連の作業として任せてよいかが不明確です。
範囲が大きすぎると、文脈の見落としと前提の誤りが工程をまたいで増幅し、人間も検証できなくなります。


## Goal（達成したいこと）

AIが自律的に実行する範囲を、品質・安全性・人間の検証可能性を損なわない単位に定めることです。


## No-goal（達成に含めないもの）

大規模な製品開発でAIを使わないことや、コード行数だけで変更の危険度を決めることは含めません。


## Requirements（要件）

- 変更の影響範囲、可逆性、検証可能性で規模を判断できること。

- 大規模案件を小さく検証可能な実装単位へ分割できること。

- 高リスクな判断を人間へ残せること。


## Proposed Design（提案する設計）

### 推奨する適用単位

現状は、AIに一連の実行を任せる範囲を、**一人または小さなチームが全体像を理解し、人間が一度で妥当性を確認できる変更単位**に止めるべきです。


実務上の標準は、次のような「数時間から数日で終えられる、一つの限定された変更」です。


- 一つの機能追加、バグ修正、または内部改善
- 原則として一つのリポジトリ、または一つのサービス内
- 既存の設計・外部との接続規約・受け入れ条件が明確
- 影響範囲を列挙でき、テスト環境で確認できる
- 問題があれば戻せる。
データ変更なら復旧手順がある

これはコード行数で決める基準ではありません。
数行でも、認可、本番データ、決済、複数サービスの整合性に触れるなら大きな変更です。
逆に、数百行でも一つの内部モジュールで、テストと利用条件が明確なら扱えます。


> 大規模な製品そのものをAIを中心とした開発の対象外にする必要はありません。
大きな開発を、上記の小さく検証可能な単位へ分割し、AIに任せるのは各単位までにする、という意味です。


### 規模を止めるべき理由

LLMは、現在与えられた文脈と学習済みのパターンから、次にもっとも自然そうな出力を生成します。
システム全体の事実を常に把握し、正しさを現実世界へ照会しているわけではありません。


変更が大きくなるほど、次の三つが同時に増えます。


```text
関係する情報が増える
      ↓
見落とし・古い情報・矛盾する情報が増える
      ↓
前提の誤りが設計、実装、テストへ伝播する
      ↓
人間が一度に検証できなくなる
```

LLMは誤った前提からでも、一貫して見える設計・コード・テストを作れます。
規模が大きいほど、その誤りは多くの成果物とサービスへ広がり、修正コストが急に上がります。
したがって、AIの生成能力よりも、**人間が前提と結果を検証できる範囲**が上限を決めます。


また、LLMには扱える文脈量の上限があります。
大量の資料を渡せても、重要な例外が埋もれたり、参照すべき依存先を選び漏らしたりします。
複数のサービス、長期の業務経緯、顧客別例外、運用上の暗黙知が絡む変更ほど、この性質が強く現れます。


### 進め方の目安

| 開発単位 | AIを中心とした開発の使い方 | 人間の関与 |
| --- | --- | --- |
| 数時間〜1日。
単一モジュールの修正 | 計画、実装、テスト、レビュー補助まで任せる | 要件と差分を確認する |
| 1〜数日。
単一サービスの機能追加 | 要件、設計、実装を成果物でつなぐ | 設計と受け入れ条件を承認する |
| 数日〜2週間。
複数サービスにまたがる変更 | 全体を人間が設計し、小さな実装単位だけをAIへ渡す | 接続規約、データ移行、段階リリースを判断する |
| 数週間以上。
新規基盤、重要業務、組織横断の変更 | AIは調査・文書化・個別実装を支援する | 目的、設計、リスク、移行、本番判断を主導する |

時間はあくまで目安です。
判定で優先するのは、変更の期間ではなく、次の条件です。


### AIに一括で任せない条件

次のどれかに当てはまる場合は、開発をさらに小さく分けるか、人間主導の設計・承認を入れます。


- 複数サービスや外部システムの接続規約を同時に変える
- データ移行、削除、整合性の回復が必要になる
- 認可、秘密情報、決済、法令、監査に関係する
- 本番停止や顧客影響のある切り替えを含む
- 仕様が曖昧、または業務上の例外が文書化されていない
- 成功条件を自動テストや観測で確認できない
- 失敗時に元へ戻す手順がない

この場合に必要なのは、AIを使わないことではありません。
AIには調査、影響範囲の洗い出し、選択肢の比較、テスト作成、文書更新を任せます。
人間は、何を正とするか、どの危険を受け入れるか、いつ本番へ出すかを決めます。


### 大きな開発を扱う構造

大きな案件は、一度に「完成させる仕事」として渡さず、次の順に分割します。


```text
人間が目的・制約・成功条件を確定
             ↓
全体設計と接続規約を人間が承認
             ↓
変更を小さな実装単位へ分割
             ↓
各単位で AI が計画 → 実装 → テスト
             ↓
人間が統合結果・監視・段階リリースを確認
```

この構造なら、プロジェクト全体が数か月規模でも、AIが自律的に扱う単位は数時間から数日に保てます。
誤りが出ても、その単位で止め、戻し、学びを次の成果物へ反映できます。


### 実務での判断質問

AIへ渡す前に、次の五つへすべて「はい」と答えられるなら、現在のAIを中心とした開発の実行単位として適しています。


1. 変更の目的と受け入れ条件を、利用者の言葉で説明できるか。

2. 影響するサービス、データ、外部接続を列挙できるか。

3. AIが参照すべき正本の資料と既存実装が分かっているか。

4. 自動テストまたは観測で、成功・失敗を判断できるか。

5. 失敗時に止める、戻す、または影響を限定する方法があるか。


一つでも「いいえ」なら、AIに長い自律実行をさせる前に、要件確認・設計・調査を先に行うべきです。


### 参考

- [AWS: AI-Driven Development Life Cycle](https://aws.amazon.com/blogs/devops/ai-driven-development-life-cycle/) — AIが計画と実行を担い、人間が重要判断を担う考え方を説明しています。

- [AWS: 適応的なAIを中心とした開発ワークフロー](https://aws.amazon.com/blogs/devops/open-sourcing-adaptive-workflows-for-ai-driven-development-life-cycle-ai-dlc/) — 作業の種類と複雑さに応じ、工程の深さを変える必要性を説明しています。

- [OpenAI: コーディング評価における信頼できる指標](https://openai.com/index/separating-signal-from-noise-coding-evaluations/) — ベンチマークの値を、そのまま実務での信頼性とみなせない理由を説明しています。

